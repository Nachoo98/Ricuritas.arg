<!DOCTYPE html>
<html lang="en">
  <%-include('../partials/head')%>
<body>
  <%-include('../partials/header')%>

    <main>

      <% for(let p of productos) {%>
            <p><%= p.id%></p>
            <p><%= p.nombre%></p>
    <% }%>

    </main>

  <%-include('../partials/footer')%>
</body>
</html>